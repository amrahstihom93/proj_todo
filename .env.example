# Google Cloud Platform Configuration
# Copy this file to .env and update with your actual values

# GCP Project Configuration
GCP_PROJECT_ID=your-project-id-here
GCP_REGION=us-central1

# Cloud Run Configuration
SERVICE_NAME=todo-app
MEMORY=256Mi
CPU=1
MAX_INSTANCES=10

# Container Registry
IMAGE_NAME=gcr.io/your-project-id-here/todo-app

# Instructions:
# 1. Copy this file: cp .env.example .env
# 2. Replace 'your-project-id-here' with your actual GCP project ID
# 3. Adjust other values as needed for your deployment
# 4. Never commit .env to version control (it's in .gitignore)
